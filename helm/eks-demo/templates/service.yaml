apiVersion: v1
kind: Service
metadata:
  name: eks-demo-service
spec:
  type: ClusterIP
  selector:
    app: eks-demo
    version: {{ .Values.activeColor }}
  ports:
  - port: 80
    targetPort: 3000
